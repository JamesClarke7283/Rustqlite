//! Built-in functions (mirrors `func.c`, `date.c`, `printf.c`).
//!
//! Placeholder: the registry of scalar and aggregate functions (`abs`, `length`, `substr`,
//! `count`, `sum`, `min`/`max`, the `date`/`time` family, …), matching the target version's
//! set and behavior exactly. Intended sub-modules: `scalar`, `aggregate`, `date`, `registry`.
//! Populated alongside the query path (M3+).
