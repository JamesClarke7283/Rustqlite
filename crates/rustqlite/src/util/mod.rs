//! Shared utilities (mirrors `util.c`, `hash.c`, `utf.c`, `global.c`).
//!
//! Placeholder for small cross-cutting helpers (hashing, UTF transforms, numeric parsing
//! quirks) as they are needed. Kept intentionally empty until something genuinely belongs in
//! more than one module — utilities accrete here rather than being invented up front.
